# Copy to .env and set your backend URL.
# Android emulator: us
e 10.0.2.2 to reach host machine.
# Chrome / iOS simulator: localhost is fine.
# Physical device: use your computer's LAN IP (e.g. 192.168.1.100).
API_BASE_URL=http://10.0.2.2:8080/